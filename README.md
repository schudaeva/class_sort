## Чудаева Светлана: Класс Car , тесты,  главный цикл

- Класс Car, реализация builder 

- Тесты 

 В программе также должны быть реализованы тесты к написанному коду. Классические тестовые библиотеки использовать не обязательно (но можно), так как мы их не изучали, достаточно просто тестов в отдельных классах, реализованных руками.

- Главный цикл и интеграция 

## Тарасова Екатерина: Сортировки (+ доп задание 1)
- SortByPowerStrategy (по мощности - число).

- SortByModelStrategy (по модели - строка).

- SortByYearStrategy (по году - число).

- Доп. задание 1 (Четные/Нечетные):

     Дополнительно к основным сортировкам реализовать эти же алгоритмы сортировки таким образом, что объекты классов будут сортироваться по какому-либо числовому полю: объекты с четными значениями этого поля должны быть отсортированы в натуральном порядке, а с нечетными – оставаться на исходных позициях.
    Название: EvenOddSortByPowerStrategy

Для кастомной сортировки разрешено использовать компаратор. Запрещено применять готовые реализации сортировки, поиска и паттернов.

## Наталия Бызова: Ввод данных разными способами и валидация данных

!! Заполнение коллекций должно осуществляться посредством стримов

1. Интерфейс DataFiller

2. Реализация RandomFiller (Заполнение рандом):

    - Генерация случайных моделей 
    
    - Генерация случайной мощности
    
    - Генерация случайного года 

3. Реализация ManualFiller (Заполнение вручную):

Поочередный ввод данных с консоли.

Валидация: Проверка, что мощность и год - положительные числа, модель не пустая. Если пользователь ввел ерунду - просить ввести заново или выкидывать исключение с сообщением.

4. Реализация FileFiller (Заполнение из файла):
Чтение файла, где каждая строка: "Мощность,Модель,Год".

Парсинг строки.

Валидация: Проверка, что файл существует, строка содержит 3 части, числа корректны. Если строка битая - пропустить её или выдать ошибку.

## Тюляев Артём: Коллекции, запись коллекций/значений в файл

1. Кастомная коллекция (Доп. задание 3*):

Класс CarArrayList, который реализует простой список на основе массива (как упрощенный ArrayList).

Методы: add(Car car), get(int index), size(), toArray()

2. Класс FileSaver

Метод для записи отсортированных коллекций/ найденных значений в файл

Режим добавления (append): каждый новый результат сортировки дописывается в конец файла, а не перезаписывает его.


## Человек 5: Многопоточный подсчет

Реализовать многопоточный метод, подсчитывающий количество вхождений элемента N в коллекцию и выводящий результат в консоль.

Класс ParallelCounter




